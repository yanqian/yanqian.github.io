# Coding Run: F028

Date: 2026-08-31

Mode: manual Coding Agent fallback requested from the project root. This run does not replace separate evaluator gating or `EVAL_PASS` evidence.

Implemented:

- Added the repository-owned standalone talk bundle at `static/talks/hey-jarvis/`:
  - `index.html`
  - `talk.css`
  - `talk.js`
  - `assets/hey-jarvis-header-background.png`
- Added static and build contract coverage in `tests/test_hey_jarvis_talk.py`.

Verification commands:

- `python3 -m unittest tests/test_hey_jarvis_talk.py`
- `git diff --check`
- `./init.sh`

Results:

- Automated verification passed: 67 Python tests, 19 publisher tests, and the production Hugo build.
- Hugo output contains `/talks/hey-jarvis/index.html`, `talk.css`, `talk.js`, and the copied artwork asset.

Manual preview status:

- Attempted local browser verification on `http://localhost:1313/talks/hey-jarvis/`.
- Browser runtime returned an explicit permission denial for localhost access, so desktop/mobile visual confirmation of clipping, overflow, and Present-mode feel remains pending.

Capability gap assessment:

- Gap type: local browser preview permission.
- Durable capability change in this feature: none. The repository already has deterministic automated source and build coverage; the remaining gap is operator/browser permission, not missing project code.
- Follow-up required: rerun evaluator or user-led manual preview once localhost browser permission is available.

CODING_PASS: F028
