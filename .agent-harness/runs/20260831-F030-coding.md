# F030 Coding Run

Date: 2026-08-31
Feature: `F030`
Mode: manual Coding Agent fallback from the project root

## Scope implemented

- Replaced the standalone talk hero reference in `static/talks/hey-jarvis/index.html` with the supplied real app screenshot asset at `static/talks/hey-jarvis/assets/hey-jarvis-app-ui.png`.
- Updated scene 4's second failure to the acknowledgement-readiness race so `"I'm here"` is emitted only after a two-condition readiness barrier.
- Aligned the standalone talk palette and typography in `static/talks/hey-jarvis/talk.css` with the site's established light/dark tokens and system sans-serif stack while preserving existing Reading and Present interactions.
- Updated the repository-owned editorial brief in `.agent-harness/docs/hey-jarvis-meetup-talk.md`.
- Extended `tests/test_hey_jarvis_talk.py` to verify the new screenshot asset/reference/alt text, corrected failure copy, site-token palette, and present-shell contrast tokens.

## Verification

- `python3 -m unittest tests.test_hey_jarvis_talk`
- `git diff --check -- static/talks/hey-jarvis .agent-harness/docs/hey-jarvis-meetup-talk.md tests/test_hey_jarvis_talk.py`
- `./init.sh`

All three commands passed.

## Browser verification status

`F030` still needs fresh desktop and 390px mobile browser verification plus independent evaluator evidence before it can be marked done.

Attempted environment checks:

- `python3 -m http.server 4173 --directory /tmp/f030-public`
  - Failed with `PermissionError: [Errno 1] Operation not permitted` when binding a local HTTP port in this environment.
- `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu --virtual-time-budget=3000 --dump-dom file:///private/tmp/f030-public/talks/hey-jarvis/index.html`
  - Exited with code `134`, so headless Chrome could not be used here for the required browser pass.

Failure domain: `environment_gap`

Harness improvement assessment:

- No repository-owned product change can remove this local verification restriction inside `F030`.
- The harness should continue treating browser acceptance as pending until a session with working browser-control or headless-browser capability records the required evaluation evidence.

CODING_PASS: F030
