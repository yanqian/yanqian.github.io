# Coding Run: F029

Date: 2026-08-31

Mode: manual Coding Agent fallback requested from the project root. This run does not replace separate evaluator gating or `EVAL_PASS` evidence.

Implemented:

- Added a new Hey Jarvis discovery link to `content/projects.md` with the label `Meetup Talk` pointing to `/talks/hey-jarvis/`.
- Added a matching discovery link to `content/projects.zh.md` with the label `英文 Meetup 分享` pointing to the same standalone English talk route.
- Extended `tests/test_projects_page.py` so `F029` verifies both source content and Hugo-rendered `/projects/`, `/zh/projects/`, and `/talks/hey-jarvis/` output.

Verification commands:

- `PYTHONPYCACHEPREFIX=/private/tmp/yanqian-github-io-pycache python3 -m unittest tests.test_projects_page`
- `git diff --check`
- `./init.sh`

Results:

- Targeted Projects tests passed: 6 tests, including a temp-destination Hugo build that verifies both Projects routes and the standalone talk destination.
- Full verification passed: 68 Python tests, 19 publisher tests, harness checks, and the production Hugo build.

Remaining:

- Independent evaluator evidence is still required before `F029` can move from `in_progress` to `done`.

CODING_PASS: F029
