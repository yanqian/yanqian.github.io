# F012 Publisher Runbook and Agent Entry Point

Manual Coding Agent fallback was used because the configured evaluator provider remains unavailable.

## Evidence

- `AGENTS.md` contains concise mandatory launcher, lock, startup-proof, install, and human-approval rules.
- `docs/publishing/localization-runbook.md` is the single operational source for normal execution and recovery.
- The incident report maps each July 2026 failure to a durable code or test control and records operator errors without treating chat as state.
- The installed vault README points back to the repository instead of duplicating the Runbook.
- Node contract tests assert that the agent entrypoint, Runbook, incident controls, and installer pointer remain discoverable.

EVAL_PASS: F012
