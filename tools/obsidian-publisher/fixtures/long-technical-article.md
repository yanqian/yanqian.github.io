## Connection Tools

### SSH

Use SSH for remote login and Tailscale for private networking.

```bash
# On the Mac
tailscale status
```

```text
Phone -> SSH -> Mac -> tmux
```

## Daily Workflow

### tmux

Keep the long-running task in a tmux session.

```json
{"command":"agent-new codex"}
```

## Troubleshooting

The command blocks above are protected artifacts. The `text` block is localizable prose.
